# 叙脉研发路线图

本文只回答“下一步先做什么、为什么按这个顺序、什么时候才算完成”。产品细节以 `DECISIONS.md`、`PRODUCT_SPEC.md`、`UX_FLOWS.md` 和 `ACCEPTANCE.md` 为准；阶段执行以 `goals/` 中对应任务书为准。

## 当前基线

- 本任务书的阶段 31 产品基线：`e55ee722bc755c0f2562895e18f36e39803c8af0`；后续接棒始终以 GitHub 最新 `main` 为准，不按该 SHA 回退。阶段 31 作品拆解 MVP 已完成并通过独立审计。
- 阶段 32 已合并至当前 `origin/main=ec2f31a`：204 tests、0 failed、0 skipped；OpenAPI 58 paths / 61 operations；旧 `/novel` 16 paths / 19 operations；Ubuntu 与 Windows CI 均通过，独立审计 P0/P1/P2/P3 均为 0。
- 已实现：独立创作、故事档案、版本恢复、六视角深度作品拆解、AI 辅助写作基本闭环。
- 未完成：独立创作体验收口、类型与文笔引擎、真实质量增益、AI 暂停介入后的隐形恢复、生产化。阶段 33 产品/架构/验收合同已冻结，功能实现尚未开始。

## 明确顺序

| 顺序 | 阶段 | 结果 | 开始前提 | 通过门槛 |
| --- | --- | --- | --- | --- |
| 1 | 阶段 32：深度作品拆解 | 在独立创作内形成可学习、可回证据的人物、剧情、伏笔、节奏、读者体验和文笔技法报告 | 阶段 31 已审计通过 | `goals/STAGE_32_DECONSTRUCTION_DEPTH.md` 全部完成，独立审计 P0/P1/P2/P3 为 0 |
| 2 | 阶段 33：独立创作体验收口 | 用真实作者流程修复编辑、章末处理、档案、版本和拆解之间的高摩擦点 | 阶段 32 数据与页面合同稳定 | Windows 真实流程无 P0/P1/P2，作者正文和版本安全不回退 |
| 3 | 阶段 34：类型蒸馏基础设施 | 建立合法可复跑的语料登记、切片、特征提取、证据、去重和评测管线 | 深度拆解 schema 稳定 | 仅用授权、公版或用户自有文本完成离线黄金集；不把原文或可还原片段带入产品包 |
| 4 | 阶段 35：双质量引擎 | 形成“类型结构引擎”和“文笔表达引擎”，后台协作但不向普通用户暴露内部链路 | 阶段 34 有可信训练/评测样本 | 离线对照和真实模型 A/B 均证明质量提升；不能只靠主观样例宣布通过 |
| 5 | 阶段 36：AI 辅助写作重构 | 蓝图确认后持续生成；作者介入即暂停，修改后后台重整上下文再续写；只在重大冲突时询问 | 双引擎达到质量门 | 唯一正式正文、作者优先、隐形恢复、重大冲突门控、风格修改可学习，真实连续章节评测通过 |
| 6 | 阶段 37：发布前硬化 | 跨平台、恢复、安全、性能、文档与真实用户验收收口 | 前述产品能力稳定 | Windows/Ubuntu CI、独立审计、真实模型质量、备份恢复和使用说明全部通过 |
| 7 | 生产发布（另行拍板） | 云端账户、正式计费、管理员和部署 | 用户单独确认范围、预算与安全责任 | 不属于当前默认授权，不能由研发任务自行启动 |

## 为什么不能跳序

深度拆解先建立对人物、剧情、节奏和语言的可验证表示；类型蒸馏再复用同一表示从多部作品归纳规律；双质量引擎在离线评测证明有效后，才接入 AI 辅助写作。若直接先改生成提示词，只会得到难以解释、无法回归、容易过拟合个别作品的临时效果。

## 每阶段共同流程

1. 总管理任务从最新 `main` 建立 `codex/stage-<编号>-<主题>` 分支，先把新增产品裁决写入 `DECISIONS.md`。
2. 架构/合同先冻结；引擎与前端按不重叠目录并行；管理任务负责集成，不让多个任务共享写目录。
3. 完成专项、全量、静态、OpenAPI、真实浏览器和失败反向验证；测试数不得少于基线，skipped 必须为 0。
4. 独立审计失败后直接回原研发任务修复，再由同一审计任务复验，不等待用户逐步催促。
5. 通过后同步现役文档，提交、推送、开 PR、等待 CI、合并 `main`；没有进入 GitHub `main` 的阶段不算完成。

## 不随路线扩张的硬边界

- 独立创作不出现 AI 代写；作品拆解只分析作者正式正文。
- AI 辅助写作只维护一条正式正文，作者修改永远优先。
- `.env`、`.novel_*`、审计现场、真实作者数据和受版权保护的原文语料不进入 GitHub。
- 不把固定热门模板强套到所有小说；事实、推断和不确定性分开，重要结论必须能回到来源证据。
- 正式支付、管理员、多人协作、公开分享、云同步和生产部署均需另行决策。

## 阶段 33 接棒

阶段 33 以当前 `origin/main=ec2f31a` 为基线，先冻结独立创作体验合同，再由不重叠的后端、前端、浏览器集成和独立审计任务实现。执行入口是 [STAGE_33_INDEPENDENT_EXPERIENCE.md](goals/STAGE_33_INDEPENDENT_EXPERIENCE.md)；合同覆盖真实 100 章作者流程、保存/章末/档案/版本/拆解上下文、失败恢复、账户隔离、键盘与双尺寸浏览器验收。阶段 32 schema、证据和七态边界不得被阶段 33 改写。
