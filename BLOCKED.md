# 当前阻塞与待定事项

## 阶段 33 合同冻结（文档/测试设计，2026-09-05）

实现阻塞：无。本分支只冻结独立创作体验的产品、架构、跨页面状态、失败恢复、数据安全和验收设计，不实现功能、不触碰前端或本地业务数据。

当前状态：`origin/main=ec2f31a` 已作为基线；阶段 33 决策已先写入 `docs/DECISIONS.md`，任务书及产品、流程、验收、架构、路线和开发文档已同步。实现任务需按任务书分工接棒，完成真实 100 章中文流程、API/并发/重启、Edge 双尺寸、可访问性和独立审计后才能进入 Draft PR。

本轮实际门禁：隔离 `.venv` 安装依赖后，`tests/run_quality_gates.py` 为 204 tests、0 failed、0 skipped，OpenAPI 58/61、旧 `/novel` 16/19；compileall、Node 语法、diff 和变更文档本地链接检查通过。阶段 33 功能、Edge、CI 和独立审计尚未执行，不能据此宣称完成。

交付记录：`codex/stage-33-independent-experience-contract` 已推送，合同提交为 `ea62a51`，Draft PR [#7](https://github.com/Reen-Ai-H/novelagent-xumai/pull/7) 等待后续复核；本分支没有把待执行的实现、浏览器或审计门禁写成通过。

保护项：`.env`、`.novel_*`、浏览器现场、审计证据、质量研究 quarantine 和其他 worktree 均保留；这些内容不是本阶段实现输入，不得读取输出、批量改写或删除。

## 阶段 32（审计通过，已合并，2026-09-05）

实现阻塞：无。架构、实现、自动化、真实浏览器、独立审计和候选双平台 CI 均已通过并进入当前 `origin/main=ec2f31a`；真实模型付费与生产部署不在本阶段执行范围。

## 阶段 31I Windows 发布收口（2026-09-04）

实现阻塞：无。Windows 迁移手册、双平台 CI 和新仓库发布流程已完成本地与 GitHub CI 门禁。

发布记录：`Reen-Ai-H/novelagent-xumai` 的 PR [#1](https://github.com/Reen-Ai-H/novelagent-xumai/pull/1) 已合并，`main` 合并提交为 `8e42cb6486e6baa66afd7f463448cb39eb51dc3d`，当前 head 的 Ubuntu/Windows CI 均通过。目标仍是不做生产部署、Release 或 Tag。

保护项：旧 worktree、分支、`.novel_*`、`.env`、审计证据和项目外质量研究均保留，不在本阶段清理。

## 阶段 31G P2 定点收口（2026-09-04）

实现阻塞：无。时间线首尾归一化和证据章节严格公开 schema 已完成专项与全量回归。

本阶段不改前端、不读写 `.env`、不触碰 `.novel_*`，按任务要求不 push、不建 PR；等待固定审计复验。

## 阶段 31E 集成状态（历史，已由阶段 31I 迁移）

实现阻塞：无。阶段 31E 已完成前端提交整合、真实本地状态验证和本地候选门禁；其发布阻塞为历史记录。

发布阻塞（历史）：旧 `aswansong/novelagent` 远端曾返回 `Repository not found`；阶段 31I 已切换到新的 `github-current` 远端。

证据限制（非实现阻塞）：当前 CUA Chrome 未暴露 1440/1024 viewport override；已保留真实 1710×887（当前重登页 1710×831）浏览器证据和实际 `scrollWidth=viewport`，未裁切或冒充目标尺寸，精确目标尺寸留给独立复核。

## 实现/安全阻塞

无。

## 必须明确标为 pending

- 离线质量门 Q2 已验证，但真实生成质量增益、产品集成和 Q3 真实模型 A/B 仍 pending；Q3 本阶段不执行。
- 文学质量阶段 23B 结论为 C，功能审计通过不等于小说质量通过。
- GitHub `main` 是 V1 开发基线，不是生产部署；未做正式支付、管理员、云端同步或公网安全承诺。

## 保留项

另一个旧 worktree 含未提交源码和 `.novel_*` 本地数据，按数据安全规则保留；项目外质量研究仍供 Q3 使用。二者不是实现阻塞，但不得作为普通临时目录删除。

## 阶段 31 后端状态（历史，2026-09-04）

当时无拆解引擎实现阻塞；阶段 31B 的锁顺序、outbox 恢复、异常失败、证据来源和公开状态合同已补齐并等待阶段审计复验。前端页面与总集成由管理任务另行负责，不是该后端分工内的阻塞；当时列为阶段 32+ 的人物弧、完整剧情线关系图、读者体验曲线和文笔技法深挖，现已由阶段 32 六视角实现覆盖。
